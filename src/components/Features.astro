---
const features = [
  {
    title: 'High-Performance Protocols',
    body: 'Zero-copy binary protocol for maximum throughput, plus Redis-compatible RESP2 for seamless integration with existing Redis tools and clients. Supports GET/SET/DEL/PING today.',
  },
  {
    title: 'Sharded, Lock-Free Engine',
    body: 'N shards (default GOMAXPROCS, configurable) each with its own goroutine and RWMutex. FNV-1a hash routing, no cross-shard coordination — near-linear scaling as you add cores.',
  },
  {
    title: 'Zero-GC Hot Path',
    body: 'GC is disabled by default (TSD_GC_PERCENT=-1). Zero-allocation code paths with reusable scratch buffers mean zero stop-the-world pauses during normal operation.',
  },
  {
    title: 'ChaCha20-Poly1305 Encryption',
    body: 'Optional at-rest encryption via ChaCha20-Poly1305 AEAD. Zero-allocation encrypt/decrypt on the hot path. Off by default — no overhead when not in use.',
  },
  {
    title: 'Efficient TTL Management',
    body: 'O(1) TTL eviction via a timing-wheel (chronometer) with per-slot locking, plus lazy eviction on read. Expired keys are never returned, even between active sweep ticks.',
  },
  {
    title: 'Cloud-Native Observability',
    body: 'Prometheus metrics exporter per shard, OpenTelemetry tracing with configurable sample ratio, pprof profiling on demand. Built for Kubernetes from day one.',
  },
];
---
<section class="section" id="features">
  <div class="wrap">
    <div class="section__head">
      <h2>Designed for Modern Applications</h2>
      <p>
        Tellstone is built to accelerate cloud-native applications by providing
        high-performance in-memory storage with flexible deployment options.
      </p>
    </div>

    <div class="features__grid">
      {features.map((f) => (
        <div class="feature">
          <div class="feature__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
              <path d="M4 12 L10 12 L12 6 L15 18 L17 12 L20 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3>{f.title}</h3>
          <p>{f.body}</p>
        </div>
      ))}
    </div>
  </div>
</section>
